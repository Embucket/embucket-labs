[package]
name = "sql-parser"
version = "0.1.0"
edition = "2021"

[lib]
name = "sql_parser_validator"
path = "src/test_parser.rs"

[dependencies]
sqlparser = { version = "0.53.0", features = ["visitor"] }
simple_logger = "5.0.0"
